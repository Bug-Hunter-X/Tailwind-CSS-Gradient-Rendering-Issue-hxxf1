```javascript
// SomeComponent.js
<div className="bg-gradient-to-r from-blue-500 to-purple-500">
  {/* ... */}
</div>
```
This code snippet uses Tailwind's gradient utility classes, but there might be cases where the gradient doesn't render correctly, or the colors are unexpected. This can happen due to various reasons such as incorrect class names, conflicting styles, or missing dependencies.